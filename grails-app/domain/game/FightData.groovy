package game

/**
 * Used to store the data of an fight
 * that is over.
 */
class FightData {

    String fightLog

    static mapping = {
        fightLog type:"text"
    }

    static constraints = {
    }
}
